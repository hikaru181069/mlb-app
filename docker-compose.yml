services:
  mongodb:
    image: mongo:8.0
    container_name: mlb-app-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

  redis:
    image: redis:7-alpine
    container_name: mlb-app-redis
    ports:
      - "6379:6379"

volumes:
  mongodb_data:
